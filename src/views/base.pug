doctype html
html(lang="en")
    head
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
        title #{pageTitle} 
    body
        header
            nav
                ul 
                    li
                        a(href="/logout") Log out
                    li
                        a(href="/login") Login    
            h1=pageTitle
        main
            block content 

    include partials/footer