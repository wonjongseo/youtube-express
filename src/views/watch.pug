extends base


block content
    div 
        p=video.description
        small=video.createdAt
    h4 
        a(href=`${video.id}/edit`) Editing &rarr;
        hr
        a(href=`${video.id}/delete`) Deleteing &rarr;